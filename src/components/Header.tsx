const Header = () => (
  <header className="bg-primary">
    <picture>
      <img
        src="./images/bg-header-mobile.svg"
        alt="background"
        className="w-full"
      />
    </picture>
  </header>
)

export default Header
